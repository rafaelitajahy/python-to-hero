# variables

message = 'Hey! Whats up?'
num = 23
pi = 3.1415

# Nomes de varáveis
"""
    * Letras minúsculas separadas por _
    * Nome de classes maiúsculas
    * Não é permitido:
        * começar com número
        * caracteres especiais
        * espaços
        * palavras reservadas:
            O Python 3 tem estas palavras-chave:
            and     del     from    None        True
            as      elif    global  nonlocal    try
            assert  else    if      not         while
            break   except  import  or          with
            class   False   in      pass        yield
            continue        finally is          raise
            def     for     lambda  return
"""

# Ordem das operações

"""
Parênteses >  Exponenciação > Multiplicação e a Divisão > Adição e a Subtração > esquerda para a direita
"""

# Operações com strings

name = ' Rafael '
last_name = 'Itajahy'
full_name = name + last_name
print(full_name)

repet = 'GO '*5
print(repet)

# Comentários

# computa a percentagem da hora que passou
percentage = (60 * 100) / 60

percentage = (60 * 100) / 60 # percentagem de uma hora


# Depuração
# Tipos de Erros
#   Erro de sintaxe
#   Erro de tempo de execução
#   Erro semântico